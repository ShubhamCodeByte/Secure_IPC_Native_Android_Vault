buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // This is the "Android Plugin" version
        classpath 'com.android.tools.build:gradle:8.2.0'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}